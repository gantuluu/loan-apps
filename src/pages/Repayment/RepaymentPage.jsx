import { useState } from 'react';
import { Block, BlockTitle, Button, List, ListItem, Radio, Sheet } from 'konsta/react';
import { useNavigate } from 'react-router-dom';
import { activeLoan, money } from '../../data/mock.js';
export default function RepaymentPage(){const navigate=useNavigate();const [open,setOpen]=useState(false);return <><Block strong inset className="text-center"><p className="m-0 text-sm text-slate-500">Amount due</p><h1 className="text-3xl font-bold">{money(activeLoan.nextPayment)}</h1><p className="text-sm text-slate-500">Due {activeLoan.dueDate}</p></Block><BlockTitle>Payment method</BlockTitle><List strong inset><ListItem title="Bank transfer" media={<Radio name="method" checked readOnly/>} subtitle="BCA virtual account"/><ListItem title="Online payment" media={<Radio name="method" readOnly/>} subtitle="Available payment providers"/></List><Block className="action-bar"><Button large rounded onClick={()=>setOpen(true)}>Continue payment</Button></Block><Sheet opened={open} onBackdropClick={()=>setOpen(false)}><div className="p-5"><h2 className="text-lg font-bold">Confirm repayment</h2><p className="text-sm text-slate-500">Pay {money(activeLoan.nextPayment)} for your active loan?</p><Button large rounded onClick={()=>{setOpen(false);navigate('/payment')}}>Confirm payment</Button></div></Sheet></>}
