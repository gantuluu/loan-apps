import { Block, BlockTitle, List, ListItem, Button, Progressbar } from 'konsta/react';
import { useNavigate } from 'react-router-dom';
import { activeLoan, money } from '../../data/mock.js';
import { StatusBadge } from '../../components/common/UI.jsx';
export default function LoanDetailPage(){const navigate=useNavigate();return <><Block strong inset><div className="flex justify-between"><div><p className="m-0 text-sm text-slate-500">Outstanding</p><h1 className="m-0 text-3xl font-bold">{money(activeLoan.outstanding)}</h1></div><StatusBadge tone="success">Active</StatusBadge></div><Progressbar progress={activeLoan.progress} className="mt-5"/><div className="mt-2 flex justify-between text-xs text-slate-500"><span>{activeLoan.progress}% paid</span><span>{activeLoan.tenor} months</span></div></Block><BlockTitle>Loan information</BlockTitle><List strong inset><ListItem title="Principal" after={money(activeLoan.principal)}/><ListItem title="Interest rate" after={`${activeLoan.rate}% / month`}/><ListItem title="Next payment" after={money(activeLoan.nextPayment)}/><ListItem title="Due date" after={activeLoan.dueDate}/></List><BlockTitle>Installment schedule</BlockTitle><List strong inset className="!mb-8"><ListItem title="Installment 1" subtitle="Paid" after="✓"/><ListItem title="Installment 2" subtitle="Due" after={money(activeLoan.nextPayment)}/><ListItem title="Installment 3" subtitle="Upcoming"/></List><Block className="action-bar"><Button large rounded onClick={()=>navigate('/repayment')}>Make repayment</Button></Block></>}
